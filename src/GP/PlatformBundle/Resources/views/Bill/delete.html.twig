{# src/GP/PlatformBundle/Resources/views/Bill/delete.html.twig #}

{% extends "GPPlatformBundle::layout.html.twig" %}

{% block title %}
  Client - {{ parent() }}
{% endblock %}

{% block gpplatform_body %}


<h2>Supprimer une facture : </h2>

  <p>
    Etes-vous certain de vouloir supprimer la facture "{{ bill.id }}" ?
  </p>

  {# On met l'id de l'annonce dans la route de l'action du formulaire #}
  <form action="{{ path('gp_platform_deleteBill', {'id': bill.id}) }}" method="post">
    <a href="{{ path('gp_platform_view', {'id': bill.id}) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à la facture
    </a>
    {# Ici j'ai écrit le bouton de soumission à la main #}
    <input type="submit" value="Supprimer" class="btn btn-danger" />
    {# Ceci va générer le champ CSRF #}
    {{ form_rest(form) }}
  </form>
  
{% endblock %}